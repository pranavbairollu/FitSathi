<?xml version="1.0" encoding="utf-8"?>
<!-- ✨ BEST PRACTICE: RelativeLayout is more efficient for this type of layout than a weighted LinearLayout. -->
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".DashboardActivity">

    <!-- Bottom navigation -->
    <!-- Placed first so its Z-order is lower, but aligned to the bottom. -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="?attr/colorSurface"
        app:itemIconTint="@color/nav_item_color"
        app:itemTextColor="@color/nav_item_color"
        app:menu="@menu/bottom_nav_menu"
        app:labelVisibilityMode="labeled"/> <!-- Explicitly set label visibility for clarity -->

    <!-- Fragment container -->
    <!-- This FrameLayout will automatically fill the space above the BottomNavigationView. -->
    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/bottom_navigation" />

</RelativeLayout>
